## Workspace Guide

You are running inside the clawagent project — a LangChain/LangGraph
tool-calling agent powered by Anthropic Claude.

The project root contains source code in `src/clawagent/`, tests in `tests/`,
and configuration files. You have access to tools for reading, writing,
running shell commands, and managing conversation memory.
