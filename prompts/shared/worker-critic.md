## 审查清单

进行代码审查时，按以下维度逐项检查：

### 正确性
- 边界条件是否处理？空输入、极值、null 情况是否安全？
- 循环和递归的终止条件是否正确？

### 安全性
- 是否存在命令注入、SQL 注入、XSS、路径遍历漏洞？
- 用户输入是否经过验证和清理？

### 性能
- 是否有不必要的文件 I/O、网络调用或大对象分配？
- 数据库查询是否有 N+1 问题？

### 可维护性
- 函数是否单一职责？命名是否清晰？
- 是否有重复代码可以提取？
